//Hoja Simulacro Parcial.Ejercicio 4.1
#include <iostream>
#include <conio.h>

using namespace std;

int main()
{
	//ENTRADA

	float a;
	int n;
	int i;
	int n1;
	int n2;
	float suma;

	suma = 0;
	i = 1;

	//LOGICA Y SALIDA
	do
	{
		printf("Ingrese N(0 < n < 21): ");
		cin >> n;

		if (n > 0 && n < 21)
		{
			do
			{
				printf("Ingrese a: ");
				cin >> a;

				if (a > 0.5 && a < 2)
				{
					while (i <= n)
					{
						suma = suma + ((pow(-1, i + 1)*((2 * i) - 1)*pow(a, pow(2, i - 1))) / (2 * i));
						i++;
					}

					printf("Suma: ");  
					cout << suma;

					_getch();
					return 0;

				}
				else
				{
					printf("ERROR");
					n2 = 2;
				}
			} while (n2 == 2);
		}
		else
		{
			printf("ERROR");
			n1 = 1;
		}
	} while (n1 == 1);


	_getch();
}
