//Hoja Simulacro Parcial.Ejercicio 4.2
#include <iostream>
#include <conio.h>

using namespace std;

int main()
{
	//ENTRADA
	int a;

	//LOGICA Y SALIDA
	cout << "Ingrese N: " << endl;
	cin >> a;

	for (int i = 1; i <= a; i++)
	{
		for (int j = 1; j <= a; j++)
		{
			if ((j == 1) || (j == a) || (i == 1) || (i == a))
			{
				cout << "* ";
			}
			else
			{
				cout << "  ";
			}
		}
		cout << endl;
	}
	_getch();
}
